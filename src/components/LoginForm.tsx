import { useState } from 'react'
import type { FormEvent } from 'react'
import { useAuth } from '../context/AuthContext'
export function LoginForm({ onForgot }: { onForgot: () => void }) { const { login } = useAuth(), [error, setError] = useState(''), [submitting, setSubmitting] = useState(false); const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const fields = new FormData(event.currentTarget); setError(''); setSubmitting(true); try { await login(String(fields.get('email')), String(fields.get('password')), fields.get('remember') === 'on') } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to sign in.') } finally { setSubmitting(false) } }; return <form onSubmit={submit}><label htmlFor="email">Work email</label><input id="email" className="text-input" name="email" type="email" placeholder="you@company.com" autoComplete="email" required /><div className="label-row"><label htmlFor="password">Password</label><button className="text-link" type="button" onClick={onForgot}>Forgot password?</button></div><input id="password" className="text-input" name="password" type="password" placeholder="Enter your password" autoComplete="current-password" required /><label className="check"><input type="checkbox" name="remember" /><span className="box"></span>Keep me signed in</label><button className="submit" type="submit" disabled={submitting}>{submitting ? 'Signing in...' : 'Sign in'}</button>{error && <p className="form-error" role="alert">{error}</p>}</form> }
